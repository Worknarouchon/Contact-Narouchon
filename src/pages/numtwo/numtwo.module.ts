import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { NumtwoPage } from './numtwo';

@NgModule({
  declarations: [
    NumtwoPage,
  ],
  imports: [
    IonicPageModule.forChild(NumtwoPage),
  ],
})
export class NumtwoPageModule {}
